"""
1) Ask for the temperature.
   a) Use `input()` to ask today's temperature.
   b) Convert the input into an integer using `int()`.

2) Choose the main outfit.
   a) Use `if` to check if the temperature is below 20.
   b) Choose a jacket for cold weather.
   c) Use `else` to choose a t-shirt for warm weather.
   d) Store the selected outfit in the `outfit` variable.

3) Ask about rain.
   a) Ask the user if it is raining.
   b) Store the answer in `is_raining`.

4) Add an umbrella reminder.
   a) Use `if` to check if the answer is "yes".
   b) Print a reminder to bring an umbrella only when it is raining.

5) Ask for wind speed.
   a) Use `input()` to ask the wind speed in km/h.
   b) Convert the input into an integer.

6) Decide about a windbreaker.
   a) Use `if` to check if wind speed is greater than 30.
   b) Choose a windbreaker for windy weather.
   c) Use `else` when the weather is calm.
   d) Store the result in `needs_windbreaker`.

7) Ask about puddles.
   a) Ask if there are puddles on the ground.
   b) Store the answer in `has_puddles`.

8) Choose the shoes.
   a) Use `if` to choose boots when there are puddles.
   b) Use `else` to choose sneakers when the ground is dry.
   c) Store the selected shoes in the `shoes` variable.

9) Print the completion message.
   a) Add a blank line.
   b) Print that the weather check is complete.

10) Print the final outfit summary.
   a) Print the weather outfit picker heading.
   b) Show the temperature, outfit, rain status, windbreaker choice, and shoes.
   c) Print a closing line to complete the summary.
"""



